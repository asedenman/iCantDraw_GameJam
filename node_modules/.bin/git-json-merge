#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../git-json-merge/bin/git-json-merge" "$@"
  ret=$?
else 
  node  "$basedir/../git-json-merge/bin/git-json-merge" "$@"
  ret=$?
fi
exit $ret
